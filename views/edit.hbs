
  <div class="d-flex flex-column-reverse" >
    <div class="p-2 d-flex justify-content-center">
      <div class="posts mb-0 mt-1 bg-light editPost" data-update={{post.id}}>
        <div class="d-flex justify-content-center mt-0">
          <img src={{post.image}} alt="post_image" class="postImg">
        </div>
        <div class="text-muted post-text mt-0">{{post.text}}</div>
        <div class="text-muted update-text">Последнее обновление:{{post.updatedAt}}</div>
      </div>
    </div>
  </div>


 
 <div class="d-flex justify-content-center mt-3 addForm">
    <form id="editForm" name="placemark">
      <h4>Редактирование щебета</h4>
     
      <div class="mb-3">
        <input type="text" name="text" id="postTextEdit" class="form-control" placeholder="Щебет">
      </div>
      <div class="mb-3">
        <input type="file" name="image" id="postImageEdit" class="form-control" placeholder="image">
      </div>

      <div class="d-flex justify-content-center mt-2">
        <button type="submit" class="btn btn-outline-success" data-update={{post.id}}>Исправить щебет</button>
      </div>
      
    </form>
  </div> 
